#include <stdio.h>
#include "utils.h"

int main() {
    int a = 5, b = 10;

    printf("Les nombres sont %d et %d.\n", a, b);

    int resultat = somme(a, b);
    printf("La somme est duititge fafa  : %d\n", resultat);

    return 0;
}
